# Ferramentas

Tribunal TJRJ: Processo (eproc) expõe 1 ferramenta (todas somente leitura).

### 1. `tribunal_tjrj_processo_eproc_consultar`
**Input**: `cpf` (opcional), `cnpj` (opcional), `numero_processo` (opcional), `nome` (opcional), `oab` (opcional)

Tribunal TJRJ: Processo (eproc), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Tribunal TJRJ: Processo (eproc)
```
